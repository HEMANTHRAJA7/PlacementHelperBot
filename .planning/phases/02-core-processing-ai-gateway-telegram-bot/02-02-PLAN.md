---
phase: core-processing-ai-gateway-telegram-bot
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/core/ai_gateway.py
  - src/models/user.py
  - src/core/prompts/v1_classification.yaml
  - tests/test_gateway.py
autonomous: true
requirements:
  - PROC-03
  - PROC-04
  - PROC-05
  - PROC-06
user_setup:
  - service: gemini-api-key
    why: "Gemini AI model access credentials"
    env_vars:
      - name: GEMINI_API_KEY
        source: "Google AI Studio API Dashboard"
must_haves:
  truths:
    - "AI Gateway uses official google-genai SDK and gemini-2.0-flash model"
    - "Structured classification and metadata extraction are validated using a Pydantic schema"
    - "Redis token bucket rate limiter evaluates Lua script atomically"
    - "Prompt versions are loaded from versioned YAML files and token usage is logged in ai_usage_logs"
  artifacts:
    - path: "src/core/ai_gateway.py"
      provides: "AI client wrapper, Redis rate-limiter, and logger"
    - path: "src/core/prompts/v1_classification.yaml"
      provides: "Classification prompt template version 1"
  key_links:
    - from: "src/core/ai_gateway.py"
      to: "src/models/user.py"
      via: "Inserts AI usage records into database"
      pattern: "AIUsageLog"
---

<objective>
Build the Centralized AI Gateway for Gemini Flash Pydantic integration, cost tracking, prompt versioning, rate limiting, and retries.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-processing-ai-gateway-telegram-bot/02-CONTEXT.md
@.planning/phases/02-core-processing-ai-gateway-telegram-bot/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement AI Gateway Client and prompt versioning</name>
  <files>src/core/ai_gateway.py, src/core/prompts/v1_classification.yaml, src/models/user.py, tests/test_gateway.py</files>
  <behavior>
    - Test 1: AI Gateway parses email successfully and returns populated Pydantic output model.
    - Test 2: AI Gateway correctly computes USD costs for gemini-2.0-flash (input: $0.075 / 1M, output: $0.30 / 1M) and writes log to database.
    - Test 3: Loads YAML prompt versions correctly.
  </behavior>
  <action>
    Create src/core/prompts/v1_classification.yaml storing prompt guidelines. Add AIUsageLog schema model in src/models/user.py. Implement AIGateway class in src/core/ai_gateway.py using google-genai library, loading YAML prompts, executing calls using config schema, calculating costs, and writing log records to PostgreSQL. Add tests.
  </action>
  <verify>
    <automated>python -m pytest tests/test_gateway.py::test_ai_gateway_parsing</automated>
  </verify>
  <done>
    AI Gateway classifies emails, extracts metadata, tracks prompt versioning, and persists usage cost logs in database tables.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Redis Lua Token Bucket Rate Limiter</name>
  <files>src/core/ai_gateway.py, tests/test_gateway.py</files>
  <behavior>
    - Test 1: Concurrently requesting tokens blocks when limit is exceeded (returns False).
    - Test 2: Rate limiter allows tokens to refill gradually over time.
  </behavior>
  <action>
    Implement Redis-based Token Bucket rate limiting using atomic Lua script inside AIGateway rate_limit_check. Integrate rate limit check inside API call retries. Add tests.
  </action>
  <verify>
    <automated>python -m pytest tests/test_gateway.py::test_rate_limiting</automated>
  </verify>
  <done>
    Redis Lua script execution manages atomic API rate limiting and delays requests successfully during burst overflows.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Prompt template YAML files loaded from disk.
- Redis broker connecting to shared cluster.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Denial of Service | src/core/ai_gateway.py | mitigate | Limit maximum retry attempts to 3 with backoffs to prevent infinite looping and excessive costs. |
</threat_model>

<verification>
Execute the pytest suite for the gateway.
</verification>

<success_criteria>
- AI Gateway accurately validates Gemini output format schemas.
- Redis-based rate limiting blocks concurrent task overflows.
- Usage costs are persisted in the PostgreSQL database.
</success_criteria>

<output>
Create `.planning/phases/02-core-processing-ai-gateway-telegram-bot/02-02-SUMMARY.md` when done
</output>
