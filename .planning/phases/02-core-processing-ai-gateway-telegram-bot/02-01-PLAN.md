---
phase: core-processing-ai-gateway-telegram-bot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/gmail.py
  - src/core/pre_check.py
  - src/tasks/email_tasks.py
  - tests/test_processing.py
autonomous: true
requirements:
  - PROC-01
  - PROC-02
user_setup: []
must_haves:
  truths:
    - "Celery task process_email_event fetches email details and body via Gmail API using decrypted refresh tokens"
    - "System runs local deterministic pre-check on email text for Register Numbers, NeoPAT IDs, and Email addresses (substring search)"
    - "Name matching splits student name into tokens and ensures at least two tokens are found case-insensitively in the text"
  artifacts:
    - path: "src/core/gmail.py"
      provides: "Gmail API message downloader"
    - path: "src/core/pre_check.py"
      provides: "Case-insensitive identifier matcher"
  key_links:
    - from: "src/tasks/email_tasks.py"
      to: "src/core/gmail.py"
      via: "Fetches Gmail message details"
      pattern: "fetch_gmail_message"
---

<objective>
Implement background Gmail email retrieval and the local deterministic pre-check matching engine.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-processing-ai-gateway-telegram-bot/02-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement Gmail API Mail Retriever</name>
  <files>src/core/gmail.py, tests/test_processing.py</files>
  <action>
    Create src/core/gmail.py implementing fetch_gmail_message(access_token, message_id). It makes an asynchronous call to the Gmail API endpoint (format=full) to download subject, headers, and the decoded plain text body (handling base64 and quoted-printable payloads). Create tests/test_processing.py with mock responses.
  </action>
  <verify>
    <automated>python -m pytest tests/test_processing.py::test_gmail_message_fetch</automated>
  </verify>
  <done>
    Gmail message downloader compiles and parses headers/bodies successfully under test mocks.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Deterministic Local Pre-Check Matcher</name>
  <files>src/core/pre_check.py, tests/test_processing.py</files>
  <behavior>
    - Test 1: Register Number (e.g. 21BCE0001) substring match returns True case-insensitively.
    - Test 2: Student name token match splits name and returns True if at least two tokens match (e.g. 'Hemanth Raja' matches 'HEMANTH RAJA S' and 'Raja Hemanth').
    - Test 3: Missing name tokens or unmatched IDs return False.
  </behavior>
  <action>
    Create src/core/pre_check.py containing check_student_identifiers(body, register_number, email, neopat_id, full_name). It performs case-insensitive search for IDs and runs name token splitting checks. Add tests in tests/test_processing.py.
  </action>
  <verify>
    <automated>python -m pytest tests/test_processing.py::test_pre_check_matching</automated>
  </verify>
  <done>
    Pre-check matcher accurately flags student records in body text and handles tokenized name splitting.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Gmail API response data crossing into Celery worker parsing thread.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | src/core/gmail.py | mitigate | Strictly parse headers and payload; sanitize inputs to prevent injection attacks during later formatting stages. |
</threat_model>

<verification>
Execute the pytest suite for processing.
</verification>

<success_criteria>
- Workers successfully fetch and decode Gmail payload components.
- Substring and token name-matching logic passes all verification tests.
</success_criteria>

<output>
Create `.planning/phases/02-core-processing-ai-gateway-telegram-bot/02-01-SUMMARY.md` when done
</output>
