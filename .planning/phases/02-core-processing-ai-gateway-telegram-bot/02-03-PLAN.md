---
phase: core-processing-ai-gateway-telegram-bot
plan: 02
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/core/telegram_dispatcher.py
  - src/tasks/email_tasks.py
  - tests/test_bot.py
autonomous: true
requirements:
  - PROC-07
  - PROC-08
user_setup:
  - service: telegram-bot-token
    why: "Telegram API message transmission credentials"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "Telegram @BotFather API Token"
must_haves:
  truths:
    - "Telegram Bot pushes formatted alert messages to mapped chat IDs using HTML format"
    - "Notification priority engine groups and styles alerts: Offer > Shortlist > Interview > Assessment > Opportunity"
    - "All messages are sent silently (disable_notification=True) to prevent user distraction"
  artifacts:
    - path: "src/core/telegram_dispatcher.py"
      provides: "Silent HTML Telegram notification service"
  key_links:
    - from: "src/tasks/email_tasks.py"
      to: "src/core/telegram_dispatcher.py"
      via: "Dispatches alerts after classification"
      pattern: "send_telegram_alert"
---

<objective>
Implement the Telegram Bot dispatcher, priority engine styles, and integrate the background email task processing pipeline.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-processing-ai-gateway-telegram-bot/02-CONTEXT.md
@.planning/phases/02-core-processing-ai-gateway-telegram-bot/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Telegram Notification Dispatcher and Priority Styling</name>
  <files>src/core/telegram_dispatcher.py, tests/test_bot.py</files>
  <behavior>
    - Test 1: Notification formatter generates correct HTML structure with bold headers and metadata table.
    - Test 2: Emoji badges correspond correctly to priorities (🔴 Offer, 🟡 Shortlist, 🔵 Interview, 🟢 Assessment, ⚪ Opportunity).
    - Test 3: API dispatcher sends request with disable_notification=True.
  </behavior>
  <action>
    Create src/core/telegram_dispatcher.py. Declare send_telegram_alert(chat_id, classification, company, role, package, deadline, link). It constructs the HTML message based on priority styles and performs an async request to Telegram API. Add tests.
  </action>
  <verify>
    <automated>python -m pytest tests/test_bot.py</automated>
  </verify>
  <done>
    Telegram Bot dispatcher sends formatted silent notifications and priority badges map correctly.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Integrate Pipeline in Celery Task</name>
  <files>src/tasks/email_tasks.py, tests/test_processing.py</files>
  <action>
    Modify process_email_event in src/tasks/email_tasks.py. Retrieve user's decrypted tokens, fetch Gmail message details, run local pre-checks, execute Gemini AI Gateway parser, and dispatch Telegram notification alerts. Adjust tests.
  </action>
  <verify>
    <automated>python -m pytest tests/test_processing.py</automated>
  </verify>
  <done>
    Celery task integrates Gmail download, matching checks, Gemini analysis, and Telegram Bot alert dispatching.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Outbound API calls to Telegram Server.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | src/core/telegram_dispatcher.py | mitigate | Ensure student register numbers and other secrets are not leaked in log headers or exception logs. |
| T-02-04 | Spoofing | src/core/telegram_dispatcher.py | mitigate | Verify Bot Token is fetched from environment configuration variables. |
</threat_model>

<verification>
Run the complete test suite.
</verification>

<success_criteria>
- Telegram Bot dispatches silent HTML notifications with priority emojis.
- Full email processing pipeline runs successfully end-to-end under test.
</success_criteria>

<output>
Create `.planning/phases/02-core-processing-ai-gateway-telegram-bot/02-03-SUMMARY.md` when done
</output>
