---
phase: 05-reminder-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/user.py
  - src/core/telegram_dispatcher.py
  - src/tasks/email_tasks.py
  - src/tasks/worker.py
  - tests/test_bot.py
  - tests/test_reminders.py
autonomous: true
requirements:
  - REM-01
user_setup:
  - service: local-postgres-redis
    why: "Local database and Celery worker environment"

must_haves:
  truths:
    - "New Reminder database model is created"
    - "All Telegram notifications are sent using normal priority (disable_notification=False)"
    - "Celery Beat schedules the reminder check every 5 minutes"
    - "Missed alerts are caught up if within 2 hours, else skipped"
  artifacts:
    - path: "src/models/user.py"
      provides: "Updated Reminder DB mapping"
    - path: "tests/test_reminders.py"
      provides: "Unit and integration tests for pruner, deadline parser, and catch-up scheduler"
  key_links:
    - from: "src/tasks/email_tasks.py"
      to: "src/core/telegram_dispatcher.py"
      via: "send_telegram_alert calls"
      pattern: "send_telegram_alert"
---

<objective>
Implement the scheduled deadline reminder engine.
We will define the `Reminder` DB model, modify the default Telegram notification delivery options to normal (non-silent) delivery, extract and store deadlines from emails on matching, implement the periodic Celery Beat task scanning active reminders every 5 minutes, and apply the 2-hour catch-up policy.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-reminder-engine/05-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement Reminder Model and Modify Telegram Alerts Delivery</name>
  <files>src/models/user.py, src/core/telegram_dispatcher.py, tests/test_bot.py</files>
  <action>
    Add the Reminder ORM model in src/models/user.py. Modify send_telegram_alert in telegram_dispatcher.py to set disable_notification=False (normal delivery). Update test_bot.py to assert disable_notification=False in mock requests.
  </action>
  <verify>
    <automated>pytest tests/test_bot.py</automated>
  </verify>
  <done>
    Reminder model is defined and the bot delivery is set to normal delivery.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement Deadline Parsing and Pipeline Integration</name>
  <files>src/tasks/email_tasks.py</files>
  <action>
    Implement parse_deadline(deadline_str) in email_tasks.py. Update process_email_pipeline_async to parse incoming email deadlines, check if the category is eligible (Opportunity, Assessment, Interview), and save a new Reminder to the DB if the deadline is in the future.
  </action>
  <verify>
    <automated>python -c "from src.tasks.email_tasks import parse_deadline; print('Parser imported')"</automated>
  </verify>
  <done>
    Deadlines are correctly extracted, parsed, and stored during email processing runs.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build Celery Beat Reminder Scheduler and Catch-Up Logic</name>
  <files>src/tasks/email_tasks.py, src/tasks/worker.py, tests/test_reminders.py</files>
  <behavior>
    - Test 1: Date parser correctly parses various ISO and custom string formats.
    - Test 2: Pipeline saves future deadlines to reminders table.
    - Test 3: Scheduler task correctly triggers 24h, 6h, and 1h alerts.
    - Test 4: Missed reminders are caught up if within 2 hours, else marked skipped.
  </behavior>
  <action>
    Implement check_and_send_reminders_task in email_tasks.py that runs every 5 minutes. Implement the catch-up check policy (2h window threshold). Register task schedule in worker.py. Write verification unit tests in tests/test_reminders.py.
  </action>
  <verify>
    <automated>pytest tests/test_reminders.py</automated>
  </verify>
  <done>
    Celery Beat periodic scheduler triggers correct alerts, verified by the test suite.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Celery Beat→Telegram | Periodic scheduler triggers outbound telegram alert calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | src/tasks/email_tasks.py | mitigate | Scheduled reminders only contain company, role, category, and deadline text; no personal student information or private email bodies are sent. |
</threat_model>

<verification>
Verify that pytest runs successfully across all tests.
</verification>

<success_criteria>
- Reminders database table maps deadlines.
- Alerts are delivered at 24h, 6h, and 1h intervals.
- Catch-up triggers missed alerts under 2 hours, skipping older ones.
- Notifications play sound (normal delivery).
</success_criteria>

<output>
Create `.planning/phases/05-reminder-engine/05-01-SUMMARY.md` when done
</output>
