---
phase: 04-lifecycle-privacy-observability
plan: 02
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - src/models/user.py
  - src/core/audit.py
  - src/api/endpoints/auth.py
  - src/tasks/email_tasks.py
  - src/tasks/worker.py
  - tests/test_lifecycle.py
autonomous: true
requirements:
  - SEC-03
  - SEC-04
user_setup:
  - service: local-postgres-redis
    why: "Local database and Celery worker environment"

must_haves:
  truths:
    - "No email content, subject, filenames, OCR outputs, prompts, or company names are recorded in the audit log"
    - "Audit logs utilize structured metadata columns exclusively"
    - "Celery task automatically purges logs and DLQ entries older than 90 days daily"
  artifacts:
    - path: "src/core/audit.py"
      provides: "Audit log creation helpers"
    - path: "src/models/user.py"
      provides: "AuditLog schema definition"
  key_links:
    - from: "src/tasks/email_tasks.py"
      to: "src/core/audit.py"
      via: "log_audit_event calls"
      pattern: "log_audit_event"
---

<objective>
Implement structured, privacy-first security audit logging and automated 90-day retention cleanup.
We will create the `AuditLog` table using structured columns for metadata (no email bodies, subjects, names, or attachment filenames), add logging helpers, write audit entries for auth callbacks, watch renewals, alerts, and errors, and schedule a daily Celery task to prune logs older than 90 days from the `audit_logs` and `dead_letter_queue` tables.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-lifecycle-privacy-observability/04-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Define AuditLog Model</name>
  <files>src/models/user.py</files>
  <action>
    Add the AuditLog class to src/models/user.py containing: id (Integer PK), user_id (Integer FK to users, nullable=True), event_type (String), status (String), message_id (String, nullable=True), resource_type (String, nullable=True), error_code (String, nullable=True), retry_count (Integer, default=0), and created_at (DateTime with timezone=True and server_default=func.now()).
  </action>
  <verify>
    <automated>python -c "from src.models.user import AuditLog; print('AuditLog loaded')"</automated>
  </verify>
  <done>
    AuditLog model is successfully registered with declarative Base.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build Audit Logger Helpers and Integrate Callbacks</name>
  <files>src/core/audit.py, src/api/endpoints/auth.py</files>
  <action>
    Implement log_audit_event(event_type, status, user_id, message_id, resource_type, error_code, retry_count) helper in src/core/audit.py. Integrate this helper in src/api/endpoints/auth.py to log successful and failed OAuth onboarding attempts.
  </action>
  <verify>
    <automated>python -c "from src.core.audit import log_audit_event; print('Audit helper loaded')"</automated>
  </verify>
  <done>
    Audit logger helper created and auth callback successfully generates audit entries.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate Auditing in Worker Pipeline and Implement Cleanup Task</name>
  <files>src/tasks/email_tasks.py, src/tasks/worker.py</files>
  <behavior>
    - Test 1: Successful email processing and notification logs audit entry.
    - Test 2: DLQ routing and watch renewals generate audit log entries.
    - Test 3: Log cleanup task deletes records older than 90 days from audit_logs and dead_letter_queue.
  </behavior>
  <action>
    Add log_audit_event calls in email_tasks.py for watch renewals, email processing notifications, and DLQ routing. Implement cleanup_old_logs task to delete log records older than 90 days. Register cleanup_old_logs in Celery Beat daily scheduler at 1 AM.
  </action>
  <verify>
    <automated>pytest tests/test_lifecycle.py</automated>
  </verify>
  <done>
    Lifecycle unit tests verify correct log generation and deletion operations.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB→Pruner | Celery beat task runs DB query to prune old logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Privacy / Info Disclosure | src/models/user.py | mitigate | Strict data minimization: Audit log schema has no text field for raw email details, ensuring PII cannot be logged. |
</threat_model>

<verification>
Ensure tests/test_lifecycle.py pass.
</verification>

<success_criteria>
- Audit logs contain structured metadata only.
- 90-day pruner deletes old audit logs and dead letter queue entries.
</success_criteria>

<output>
Create `.planning/phases/04-lifecycle-privacy-observability/04-02-SUMMARY.md` when done
</output>
