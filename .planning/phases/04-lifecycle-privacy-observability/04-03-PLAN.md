---
phase: 04-lifecycle-privacy-observability
plan: 03
type: execute
wave: 1
depends_on: ["04-02"]
files_modified:
  - requirements.txt
  - src/core/metrics.py
  - src/core/ai_gateway.py
  - src/core/telegram_dispatcher.py
  - src/api/endpoints/webhook.py
  - src/api/endpoints/monitoring.py
  - src/api/router.py
  - src/main.py
  - src/tasks/email_tasks.py
  - Dockerfile
  - tests/test_observability.py
autonomous: true
requirements:
  - SEC-05
user_setup:
  - service: local-postgres-redis
    why: "Local database and Celery worker environment"

must_haves:
  truths:
    - "Exposes GET /metrics endpoint returning standard Prometheus metrics format"
    - "GET /api/v1/health endpoint actively checks PostgreSQL, Redis, Celery heartbeat, and renewal freshness"
    - "Dockerfile installs system packages including tesseract-ocr for image scans"
  artifacts:
    - path: "src/core/metrics.py"
      provides: "Prometheus metrics definitions"
    - path: "src/api/endpoints/monitoring.py"
      provides: "Monitoring API routes"
    - path: "Dockerfile"
      provides: "Production Docker instructions"
  key_links:
    - from: "src/api/endpoints/monitoring.py"
      to: "src/core/metrics.py"
      via: "Gauge updates before scrapings"
      pattern: "active_watches_total"
---

<objective>
Configure Prometheus metrics telemetry, component health checks, and production Dockerfile configurations.
We will define and implement all 13 required Prometheus metrics, register incremental hooks across the webhook, AI gateway, Telegram dispatcher, and Celery task pipelines, design GET `/metrics` and JSON GET `/api/v1/health` API endpoints, and create a production Dockerfile installing system-level `tesseract-ocr`.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-lifecycle-privacy-observability/04-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Define Prometheus Metrics and Increment Hooks</name>
  <files>requirements.txt, src/core/metrics.py, src/core/ai_gateway.py, src/core/telegram_dispatcher.py, src/api/endpoints/webhook.py, src/tasks/email_tasks.py</files>
  <action>
    Add prometheus-client to requirements.txt. Create src/core/metrics.py declaring the 13 required metrics (watches, AI cost/tokens, notifications, DLQ, webhook events, processing/parsing failures, matches, Celery queue length, watch failures). Update AIGateway, webhook, telegram dispatcher, and Celery tasks to increment the appropriate counters.
  </action>
  <verify>
    <automated>python -c "import prometheus_client; from src.core.metrics import AI_COST_USD; print('Metrics loaded')"</automated>
  </verify>
  <done>
    Prometheus metrics are defined and correctly integrated into the pipeline.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Metrics and Health Check Endpoints</name>
  <files>src/api/endpoints/monitoring.py, src/api/router.py, src/main.py, tests/test_observability.py</files>
  <behavior>
    - Test 1: GET /metrics returns prometheus text format containing active watch counts and queue metrics.
    - Test 2: GET /api/v1/health returns 200 OK with Postgres, Redis, Celery, and Watch status when all are healthy.
    - Test 3: GET /api/v1/health returns 503 Service Unavailable when any underlying dependency fails.
  </behavior>
  <action>
    Create src/api/endpoints/monitoring.py with GET /metrics (queries DB active watches and Redis queue length, returning plaintext) and GET /api/v1/health (actively verifies PG, Redis, Celery heartbeat, and watch log history). Register routers in router.py and main.py.
  </action>
  <verify>
    <automated>pytest tests/test_observability.py</automated>
  </verify>
  <done>
    Both telemetry endpoints are fully functional, verified by unit tests under various health scenarios.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create Production Dockerfile</name>
  <files>Dockerfile</files>
  <action>
    Create Dockerfile in root folder: use python:3.11-slim, install apt-get dependencies including tesseract-ocr, install requirements.txt, copy source directory, and define CMD running uvicorn server.
  </action>
  <verify>
    <automated>python -c "import os; assert os.path.exists('Dockerfile'); print('Dockerfile exists')"</automated>
  </verify>
  <done>
    Dockerfile exists in root directory with correct system packages configured.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet→FastAPI | GET /metrics and GET /api/v1/health exposed publicly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Denial of Service | src/api/endpoints/monitoring.py | mitigate | Health checks run simple/fast queries (SELECT 1, redis.ping) to limit resource consumption during scrapes. |
</threat_model>

<verification>
Ensure tests/test_observability.py pass.
</verification>

<success_criteria>
- Metrics endpoint returns correct Prometheus text structure.
- Health endpoint returns JSON indicating status of all system layers.
- Dockerfile contains Tesseract OCR package installation instructions.
</success_criteria>

<output>
Create `.planning/phases/04-lifecycle-privacy-observability/04-03-SUMMARY.md` when done
</output>
