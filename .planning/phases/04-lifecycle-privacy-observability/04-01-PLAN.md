---
phase: 04-lifecycle-privacy-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/user.py
  - src/core/gmail.py
  - src/tasks/worker.py
  - src/tasks/email_tasks.py
  - tests/test_lifecycle.py
autonomous: true
requirements:
  - INGST-07
  - INGST-08
user_setup:
  - service: local-postgres-redis
    why: "Local database and Celery worker environment"
    env_vars:
      - name: GMAIL_PUBSUB_TOPIC
        source: "Set standard topic string, e.g. projects/placement-sentinel/topics/gmail-push"

must_haves:
  truths:
    - "User model includes columns for tracking active watches and expiration"
    - "Scheduled daily Celery Beat task triggers user watch renewals"
    - "Failed watch renewals are retried 3 times with exponential backoff before being marked inactive and triggering a Telegram warning alert"
  artifacts:
    - path: "src/models/user.py"
      provides: "Updated User columns"
    - path: "src/tasks/email_tasks.py"
      provides: "Watch renewal background tasks"
  key_links:
    - from: "src/tasks/email_tasks.py"
      to: "src/core/gmail.py"
      via: "setup_gmail_watch calls"
      pattern: "setup_gmail_watch"
---

<objective>
Implement background daily Gmail Watch renewal using Celery Beat.
We will update the database model for Users to store watch status and expiration details, define Gmail Watch helper methods in `gmail.py`, schedule daily renewal triggers, and implement retry logic with exponential backoff (retrying 3 times) before falling back to marking the watch inactive and notifying the user via a silent Telegram message.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-lifecycle-privacy-observability/04-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Gmail Watch tracking fields to User Model</name>
  <files>src/models/user.py</files>
  <action>
    Add columns `watch_active` (Boolean, default=True), `watch_resource_id` (String(255), nullable=True), and `watch_expiration` (BigInteger, nullable=True) to the User model.
  </action>
  <verify>
    <automated>python -c "from src.models.user import User; print('Model fields verified')"</automated>
  </verify>
  <done>
    User class contains the newly added columns without compilation errors.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement setup_gmail_watch API function</name>
  <files>src/core/gmail.py</files>
  <action>
    Add setup_gmail_watch(access_token: str, topic_name: str) -> dict function to send a POST request to users.me.watch with the topicName and labelIds=['INBOX']. Return the JSON response containing the watch expiration timestamp.
  </action>
  <verify>
    <automated>python -c "from src.core.gmail import setup_gmail_watch; print('Function import OK')"</automated>
  </verify>
  <done>
    Function is defined and successfully interacts with Gmail's watch endpoint.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Watch Renewal Tasks and Retry Policies</name>
  <files>src/tasks/email_tasks.py, src/tasks/worker.py</files>
  <behavior>
    - Test 1: Successful renewal task updates watch_active, watch_resource_id, watch_expiration in DB.
    - Test 2: Watch renewal task failure retries up to 3 times with exponential backoff.
    - Test 3: Watch renewal failure after all retries marks watch_active=False and sends silent Telegram message.
  </behavior>
  <action>
    Define renew_all_watches_task that queries users and enqueues renew_single_user_watch_task for each user. Implement renew_single_user_watch_task with max_retries=3. Upon final failure, mark watch_active=False and send silent Telegram warning message to the user. Register the task in Celery Beat daily schedule.
  </action>
  <verify>
    <automated>pytest tests/test_lifecycle.py</automated>
  </verify>
  <done>
    Unit tests confirm success and failure/retry paths for watch renewal.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Celery→Gmail API | Watch renewal triggers outbound Google API calls containing encrypted OAuth keys decrypted in memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | src/tasks/email_tasks.py | mitigate | Decrypted refresh token is strictly held in memory and never written to logs or persistent storage. |
</threat_model>

<verification>
Verify that tests in tests/test_lifecycle.py pass.
</verification>

<success_criteria>
- Daily celery beat schedule triggers watch renewals.
- Users table contains watch status and expiration.
- Warning alerts are sent silently to Telegram when watch renewals fail permanently.
</success_criteria>

<output>
Create `.planning/phases/04-lifecycle-privacy-observability/04-01-SUMMARY.md` when done
</output>
