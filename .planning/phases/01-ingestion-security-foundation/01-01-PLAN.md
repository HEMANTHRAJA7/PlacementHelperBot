---
phase: 01-ingestion-security-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - requirements.txt
  - src/core/config.py
  - src/core/database.py
  - src/core/security.py
  - src/models/user.py
  - tests/conftest.py
autonomous: true
requirements:
  - SEC-02
user_setup:
  - service: local-postgres-redis
    why: "Local database and Celery worker environment"
    env_vars:
      - name: AES_SECRET_KEY
        source: "Generate via Python: cryptography.fernet.Fernet.generate_key().decode()"
      - name: DATABASE_URL
        source: "postgresql+asyncpg://postgres:postgres@localhost:5432/placement_sentinel"
      - name: REDIS_URL
        source: "redis://localhost:6379/0"

must_haves:
  truths:
    - "System starts PostgreSQL and Redis via Docker Compose"
    - "Application loads configuration settings and AES key from environment variables"
    - "Credential encryptor performs reversible AES-256 Fernet operations"
    - "D-05: Background Celery workers can decrypt credentials out-of-band to query Gmail API headless"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Local DB and Cache services"
    - path: "src/core/config.py"
      provides: "Pydantic settings parser"
    - path: "src/core/security.py"
      provides: "Fernet cryptographic wrapper"
    - path: "src/models/user.py"
      provides: "User mapping tables"
  key_links:
    - from: "src/models/user.py"
      to: "src/core/security.py"
      via: "Encryption calls before db write"
      pattern: "encryptor\\.encrypt"
---

<objective>
Scaffold the local development environment (Docker Compose for Postgres & Redis) and implement the security foundation, consisting of the configuration loader, the database session manager, the User ORM models, and the AES-256 Fernet credential encryptor (per D-04).

Purpose: Enable secure data storage of sensitive user keys at rest.
Output: Initial boilerplate, Docker files, configuration validation, User models, and cryptographic encryption helpers.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ingestion-security-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold Local Environment and Configurations</name>
  <files>docker-compose.yml, requirements.txt, src/core/config.py, src/core/database.py</files>
  <action>
    Define docker-compose.yml mounting official PostgreSQL 16 and Redis 7 containers. Create requirements.txt including core stack packages. Build src/core/config.py using pydantic-settings to load database url, redis url, and base64 Fernet key 'AES_SECRET_KEY' (implementing D-04). Create src/core/database.py utilizing async SQLAlchemy engine mapping to PostgreSQL.
  </action>
  <verify>
    <automated>docker compose config &amp;&amp; python -c "import pydantic; print('Pydantic OK')"</automated>
  </verify>
  <done>
    Docker configuration passes linting, stack dependencies install cleanly, and FastAPI settings validate presence of AES_SECRET_KEY without crash.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Define User Model and Database Boilerplate</name>
  <files>src/models/user.py, tests/conftest.py</files>
  <action>
    Implement user.py declaring a SQLAlchemy declarative base. The User model must include columns: id (integer PK), telegram_id (bigint unique index), gmail_address (string), encrypted_refresh_token (text), encrypted_register_number (string), encrypted_neopat_id (string), created_at (timestamp). Setup conftest.py providing a session scope fixture for db testing.
  </action>
  <verify>
    <automated>python -c "from src.models.user import User; print('Model loaded')"</automated>
  </verify>
  <done>
    User class loaded without compiler error and tests/conftest.py provides mock database connection fixtures.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement AES-256 Fernet Cryptographic Helper</name>
  <files>src/core/security.py</files>
  <behavior>
    - Test 1: Initialize helper with valid 32-byte key succeeds, wrong size raises exception.
    - Test 2: Encrypting string returns different bytes and does not leak plaintext.
    - Test 3: Decrypting ciphertext returns the original plaintext accurately.
  </behavior>
  <action>
    Create src/core/security.py implementing CredentialEncryptor class using cryptography.fernet.Fernet. Fernet wraps AES-256 in CBC mode with HMAC-SHA256. Retrieve key from config.py settings.
  </action>
  <verify>
    <automated>pytest tests/test_auth.py::test_db_encryption</automated>
  </verify>
  <done>
    CredentialEncryptor successfully encrypts and decrypts secret inputs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| environment→FastAPI | Server configuration injected at container initialization; must not leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | src/core/security.py | mitigate | Enforce AES-256 Fernet authenticated encryption; verify key is retrieved only from env variables. |
| T-01-SC | Tampering | pip installs | mitigate | All external dependencies are marked ASSUMED; installer checks package validity on PyPI. |
</threat_model>

<verification>
Ensure Docker compose services run successfully. Verify pytest can execute in the root directory.
</verification>

<success_criteria>
- PostgreSQL and Redis containers boot up and accept connections.
- Cryptographic wrapper successfully encrypts and decrypts test credentials.
- SQLAlchemy ORM maps the User fields accurately.
</success_criteria>

<output>
Create `.planning/phases/01-ingestion-security-foundation/01-01-SUMMARY.md` when done
</output>
